

/**
 *
 * array_view, this function needs to
 * extract specific set of elements from numsArray
 * Given a start and end, it will need to extract
 * the values from numsArray within that range
 *
 * Example:
 * numsArray = [1, 2, 3, 4, 5, 6, 7, 8]
 * res = array_view(numsArray, 2, 5);
 *
 * res will equal = [3, 4, 5, 6]
 *
 *
 */
function array_view(numsArray, start, end) {
  

  //TODO:
  // You will need to use the parameters
  // And a loop
  // And an array with the push method

  return [];
}

module.exports = array_view;
