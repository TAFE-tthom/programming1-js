
# Drawing a Box!

You have been given a task to draw a box on the screen. This box will be made up of `*` characters.

The box should be 5 characters wide and 6 characters long. Each row should contain 5 `*` characters.

Use the `console.log` function to perform this.


## How to Test

You can test your solution by running `npm run test` which will compare your solution against the provided test cases.

