//1. Break apart lines into new line componenets
    // split('\n')

//2. (We have our array of lines/strings)
    //  words[0]
    // Example: "Good evening today!", word: 'today'
    // Prior:  split on space

//3. Iterate through the array of `derived_words from line
    // check if words[__] exists
    
//4. Given a map, if word exists in a line, check if
    //word is in map, if not , initialise that entry with
    // word and value []
    //otherwise, add line number to array
    
    // Addendum: 
    // Use line index to know what line the word was spotted in
    // Add to exist array

//5. Return map